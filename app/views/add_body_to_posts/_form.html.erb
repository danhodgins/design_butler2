<%= form_with(model: add_body_to_post, local: true) do |form| %>
  <% if add_body_to_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(add_body_to_post.errors.count, "error") %> prohibited this add_body_to_post from being saved:</h2>

      <ul>
      <% add_body_to_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :add_body_to_post_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
