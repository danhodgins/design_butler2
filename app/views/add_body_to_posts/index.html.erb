<p id="notice"><%= notice %></p>

<h1>Add Body To Posts</h1>

<table>
  <thead>
    <tr>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @add_body_to_posts.each do |add_body_to_post| %>
      <tr>
        <td><%= add_body_to_post.body %></td>
        <td><%= link_to 'Show', add_body_to_post %></td>
        <td><%= link_to 'Edit', edit_add_body_to_post_path(add_body_to_post) %></td>
        <td><%= link_to 'Destroy', add_body_to_post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Add Body To Post', new_add_body_to_post_path %>
