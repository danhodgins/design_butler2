<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :headline %>
    <%= form.text_area :headline, id: :post_headline %>
  </div>

  <div class="field">
    <%= form.label :sub_headline %>
    <%= form.text_area :sub_headline, id: :post_sub_headline %>
  </div>
  
  <div class="field">
    <%= form.label :body %>
    <%#= form.text_area :body, :class => "tinymce", id: :post_body,  %>
<%= text_area_tag :content, "Hello, World!", :class => "tinymce" %>
 <%= tinymce %>

  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>



